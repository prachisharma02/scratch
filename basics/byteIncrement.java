public class byteIncrement {
    public static void main(String[] args){
            byte b = 10;
            byte a=21;
        //  b =b+ 1; 
        //  System.out.println(b);
        //  byte c = b + 20; // whenever we perform +,-,*,/ then it will give max of(int,datatype of first value, 
        //                      datatype of second )

        //  c=b+a;                 ERROR: so here it is max(int,byte,int) then it will return int datatype 
        //                               which cant be stored in byte 
        
        //  byte  c =(byte)(a+b);// this way can be done
        //  System.out.println(c);
       
       
      
    }
}
